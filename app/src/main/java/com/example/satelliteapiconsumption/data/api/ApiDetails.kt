package com.example.satelliteapiconsumption.data.api

object ApiDetails {
    //Inter national space station current location
    // API -> GET http://api.open-notify.org/iss-now.json

    //base url and endpoint
    const val BASE_URL = "http://api.open-notify.org/"
    const val ENDPOINT_URL = "iss-now.json"
    
}